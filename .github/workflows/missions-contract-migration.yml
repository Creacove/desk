name: Missions contract migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migration complete; workflow retained only until pre-deploy cleanup."

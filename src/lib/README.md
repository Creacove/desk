# Shared Runtime Library

Shared runtime helpers belong here, including Supabase clients, auth helpers, common utilities, and small framework-neutral helpers.

Keep product workflow logic in `src/services/`.
